def num_countries_in_days(max_days, factor):
    time_left = max_days
    count = 0
    time_in_country = 1
    # while time_left >= time_in_country:
        # count += 1  # Visit the current country
        # time_left -= time_in_country  # Deduct the time spent in the current country
        # time_in_country *= factor  # Increase the time for the next country visit
#
    # return count
# ?
